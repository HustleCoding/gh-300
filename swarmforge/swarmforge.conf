# SwarmForge + Cursor CLI — quiz-app redesign
# Run from repo root:
#   SWARMFORGE_CURSOR_MODEL=<your-model> /path/to/swarm-forge/swarmforge.sh "$(pwd)"
#
# Requires: tmux, git, zsh; `cursor` on PATH (Cursor → Install shell command); cursor agent available.

window architect cursor master
window coder cursor coder
window reviewer cursor reviewer
window logger none none
